import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("tik tok toes")
                .font(.system(size:36))
                .fontWeight(.heavy)
        }
        
        .padding()
    }
}

#Preview {
    ContentView()
}
